const ROOT_PLACES = document.getElementById('places');
const ROOT_COUNTER = document.getElementById('counter');
const ROOT_START = document.getElementById('start')
const ROOT_AUTH = document.getElementById('auth')